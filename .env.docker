# Docker Compose Environment Configuration
# This file is used when running with docker-compose

# Backend API - Use service name from docker-compose.yml
# The 'backend' hostname will be resolved by Docker's internal DNS
VITE_API_BASE_URL=http://backend:8000/v1

VITE_ENV=production
VITE_DEBUG=false
